const { sequelize, Sequelize } = require(".");

// should find the way to deal with address and photo
module.exports = (sequelize, Sequelize) => {
    const FundRaise = sequelize.define("fundraise", {
        
    }
}